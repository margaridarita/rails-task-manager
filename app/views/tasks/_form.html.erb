<%= form_with model: task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title%>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit %>
<% end %>

<%# don't forget to transfrom the model name into instance variable if it's not here %>
